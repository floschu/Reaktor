apply from: "$rootDir/library-module.gradle"

dependencies {
    api project(':reaktor-core')
    api(Libs.lifecycle_extensions)
    api(Libs.lifecycle_reactivestreams_ktx)
    implementation(Libs.rxandroid)
}